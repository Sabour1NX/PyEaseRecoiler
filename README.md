# PyEaseRecoiler
Windows-only mouse recoil compensation tool (requires admin rights) with two modes: Simple Mode (single-parameter) and Complex Mode (multi-stage config via editor). Licensed under GNU GPLv2.
